<%= form_with(model: transbank, local: true) do |form| %>
  <% if transbank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transbank.errors.count, "error") %> prohibited this transbank from being saved:</h2>

      <ul>
      <% transbank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="field">
    <%= form.label :method_id %>
    <%= form.text_field :method_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
